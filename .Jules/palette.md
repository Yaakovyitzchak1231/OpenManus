## 2026-01-27 - Testing Embedded Frontend
**Learning:** Testing embedded HTML in Python strings requires extraction and manual loading into Playwright. The pattern of unescaping `\"` is critical.
**Action:** Use script to extract HTML, replace `\"`, and load via `file://` for testing.
